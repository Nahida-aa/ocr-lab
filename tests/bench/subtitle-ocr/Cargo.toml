[package]
name = "bench-subtitle-ocr"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "subtitle-ocr 三实现（cpp/py/rust）的准确性与速度基准测试驱动"

[dependencies]
serde_json = { workspace = true }
# 跨包流程例子（sf_ocr bin）：subtitle-finder 提关键帧 → subtitle-ocr 识别。
subtitle-finder = { workspace = true }
subtitle-ocr = { workspace = true }
# ModelProfile（subtitle-ocr 的 from_profile 入参）。
rapidocr-ort = { workspace = true }
anyhow.workspace = true
# sf 基准：把 subtitle-finder 关键帧原始帧存 PNG。
image = "0.25"
# sf 基准：关键帧 frame 是 ndarray Array3。
ndarray = "0.17"

[lib]
name = "bench_subtitle_ocr"
path = "src/lib.rs"
