# 让 release 构建用 AVX2 基线使标量循环被 LLVM 自动向量化；对齐 g++ 对照基准。
# 不用 -C target-cpu=native：实测无性能差别（甚至更差），且会发射无运行时守卫的
# AVX-512 指令，产物不跨机器可移植。AVX2 是 2013 年后的 x86-64 普遍具备。
# 手写 SIMD 热点（geometry imgproc）仍经 is_x86_feature_detected 运行时分派。
[build]
rustflags = ["-C", "target-feature=+avx2"]
