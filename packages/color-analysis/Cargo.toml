[package]
name = "color-analysis"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "color_analysis"
path = "src/lib.rs"

[dependencies]
# 图像读取与像素访问（RgbImage / RgbaImage）。
image = "0.25"
# 错误链。
anyhow.workspace = true
